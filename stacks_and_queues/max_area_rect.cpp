#include<bits/stdc++.h>
using namespace std;
int maxarea(vector<int>v , int n)
{
    stack<int> s;
    for(int i=n-1;i>=0;i--)
    {
        int el_index = i;
        if(i==0)
        {

            lb = -1;
        }
        while(!s.empty() )
            (arr[j] < )

    }
}
int main()
{

    int n;
    cin>>n;
    vector<int> v;
    for(int i=0;i<n;i++)
    {
      int num;
      cin>>num;
      v.push_back(num);
    }
}
